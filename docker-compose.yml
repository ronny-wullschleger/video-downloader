services:
  celluloid:
    build: .
    image: celluloid:local
    ports:
      - "127.0.0.1:8765:8765"
    environment:
      CELLULOID_DOWNLOAD_DIR: /downloads
    volumes:
      - ${USERPROFILE}/Downloads:/downloads
    init: true
